# Swarm Investigation Findings

## 1. `.beads/` directory contents
```
PRIME.md
README.md
backup
config.yaml
dolt
dolt-server.port
hooks
interactions.jsonl
last-touched
metadata.json
push-state.json
```

## 2. Beefcake file search
Found: `./.bd/issues/beefcake-h9axw.md`
Note: The search found beefcake-h9axw.md, not beefcake-k63rm.md

## 3. `.beads/beefcake-k63rm.md`
Result: NOT FOUND (file does not exist in .beads/)

## 4. `bd show beefcake-k63rm`
Result: SUCCESS - Issue exists and is IN_PROGRESS (P1 priority)

**Issue Summary:**
- Title: "Run formal TZ evaluations for worker_code_edit and code_fixing"
- Owner: Brian Squires
- Status: IN_PROGRESS
- Created: 2026-04-07, Updated: 2026-04-12

**Key Finding - Postmortem (2026-04-12T21:08:07Z):**
The agent completely misunderstood the task. Instead of actually running TensorZero evaluations using the CLI or scripts, it only updated the swarm checkpoint JSON file and posted a progress note — it never executed a single `tensorzero eval run` command or collected any real evaluation data. The agent then stalled in a read loop after its trivial file edits, triggering the post-write stall termination.

**Pattern:** Task misinterpretation / simulation failure — the agent performed bureaucratic bookkeeping (updating state machine metadata) as a proxy for the actual work, never invoking the external tooling the issue explicitly required.

## 5. Swarm checkpoint files
Both files exist:
- `.swarm-checkpoint.json` (2099 bytes, modified Apr 12 22:21)
- `.swarm-session.jsonl` (4561 bytes, modified Apr 12 22:21)

## 6. `crates/swarm-agents/src/orchestrator/mod.rs` lines 4480-4530
Contains the `SwarmResumeFile` struct definition and `check_for_resume()` function:
- `SwarmResumeFile` is a serde-serializable struct for saving session state after SLURM preemption or crash
- Written to `.swarm-resume.json` in repo root
- Contains: issue, worktree_path, iteration, escalation_summary, current_tier, total_iterations, saved_at
- `check_for_resume()` reads and parses this file, returning `Option<SwarmResumeFile>`

## 7. `crates/swarm-agents/src/orchestrator/mod.rs` lines 210-250
Contains the `process_issue()` function:
- Uses state-machine driver when `SWARM_STATE_DRIVER=1` (line 223 logs "Using state-machine driver (authoritative)")
- Loads language profile from repo root
- Creates `OrchestratorContext` and calls `crate::driver::drive()`
- Handles outcome via `crate::driver::handle_outcome()`

## 8. `cargo check -p swarm-agents`
Result: SUCCESS - "Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.20s"
No compilation errors or warnings.

## Summary
The investigation reveals that issue beefcake-k63rm is about running TensorZero evaluations that were never actually executed by a previous agent run. The agent performed "bureaucratic bookkeeping" instead of running the actual evaluation commands. The code in orchestrator/mod.rs shows the current implementation uses a state-machine driver approach. The swarm checkpoint files exist and were recently modified, suggesting the agent did update state files but didn't complete the actual work.

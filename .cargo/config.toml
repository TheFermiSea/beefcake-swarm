# Local rig-core patch: saturating_sub fix for token arithmetic overflow
# when proxied models return total_tokens < prompt_tokens.
# See PR #11 for details.
# NOTE: patch directory is incomplete; using registry version instead.

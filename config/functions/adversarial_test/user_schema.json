{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "diff": { "type": "string", "description": "Git diff of changes to attack" },
    "api_signatures": { "type": "string", "description": "Public API signatures from the changed code" }
  },
  "required": ["diff"],
  "additionalProperties": false
}

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "plan": { "type": "string", "description": "Structured JSON repair plan from planner" },
    "target_files": { "type": "string", "description": "Comma-separated file paths from the plan" }
  },
  "required": ["plan"],
  "additionalProperties": false
}
